<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sample.saleapp">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".di.TheApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.BottomNavActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <service android:name=".services.ProductPurchaseService" />
        <service android:name=".services.ProductManageService" />
        <service android:name=".services.DataExportImportService" />
    </application>
</manifest>
